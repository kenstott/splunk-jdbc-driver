# ⚠️ Deprecated

This repository has been migrated into the [kenstott/calcite](https://github.com/kenstott/calcite) monorepo.

**Use `jdbc:splunk:` from the `splunk` module in kenstott/calcite instead.**

This repo is archived and no longer maintained.

## Migration

The driver is now at:
```
org.apache.calcite.adapter.splunk.SplunkDriver
```

JDBC URL: `jdbc:splunk:url=https://localhost:8089;user=admin;password=changeme`

